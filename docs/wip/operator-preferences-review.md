# Operator preferences — consolidated for review

> Uncommitted review artifact. Every place across the global agent rules and
> the per-project memory stores where the operator states a preference, rule,
> or correction. Descriptions and quotes are **verbatim** — nothing paraphrased.
> Generated by `/tmp/extract_operator_prefs.py`. Source of truth stays the
> original files; this is a read-only lens for auditing what governs the agents.

Generated: 2026-07-24

---

## A. Global codified rules (already active in every session)

These are the operator asks that have already been distilled into hard rules.
Full text lives in the two files below; this is the index of rule headlines.

### `~/.config/AGENTS.md` — 13 rules

- 0.1 — Direct answers first.
- 0.2 — Evidence-first claims.
- 0.3 — Ambiguous evidence is inconclusive.
- 0.4 — Uncertainty is named, not hedged.
- 0.5 — Reason-first when the operator asks why.
- 0.6 — Coverage claims require a "not covered" section of equal
- 0.7 — No cargo-cult suppression.
- 0.8 — Banned self-flattering phrases — never use unless the
- 0.9 — No pivot to a related task in place of an answer.
- 0.10 — Speed is not a virtue.
- 0.11 — Watch running tasks live.
- 0.12 — Pre-send draft-scan is MANDATORY, not aspirational.
- 0.13 — No invented scope.

### `~/.claude/CLAUDE.md` — 13 rules

- T1 — Direct answers first.
- T2 — Evidence-first claims.
- T3 — Ambiguous evidence is inconclusive.
- T4 — Uncertainty is named, not hedged.
- T5 — Reason-first when the operator asks why.
- T6 — Coverage claims require a "not covered" section of equal
- T7 — No cargo-cult suppression.
- T8 — Banned self-flattering phrases unless the citation is
- T9 — No pivot to a related task in place of an answer.
- T10 — Speed is not a virtue.
- T11 — Watch running tasks live.
- T12 — Pre-send draft-scan is MANDATORY.
- T13 — No invented scope.

---

## B. Memory feedback rules (the origin — 'the operator said X')

Each entry: **the rule** (verbatim `description`) + the operator's own words
where recorded. Source path in backticks.

### orrery — 72 feedback rules

#### feedback-adr073-layer-a-deferred-v09
"ADR-073 Layer A (per-region insets) is off the table for v0.7 / v0.8 — do not propose, plan, or scaffold Layer A work until v0.9 epic opens (re-eval 2026-Q4)."
> "Layer A still unimplemented"
`orrery/memory/feedback_adr073_layer_a_deferred_v09.md`

#### answer-yes-no-first
"When the operator asks a direct yes/no question, lead with the answer and stop. Don't run diagnostics in parallel and offer them as \"here's what I checked\" when he just wanted the verdict. Diagnostics go after the answer, only if asked."
> "did you write that file?"
> "is it on this branch?"
> "while I'm here let me also look at X"
> "is it possible that by mistake you wrote docs/provenance/last-fetch-diff.md in other worktree?"
> "why do you ask me all these things, I just asked you if you wrote that file?"
> "No, I didn't write to that file."
`orrery/memory/feedback_answer_yes_no_first.md`

#### Check known blockers before triggering an action — say no when prerequisites aren't met
When the user proposes an action ("shall we X?"), check the plan/issue for unmet prerequisites and decline if they're not done — don't trigger and watch it fail.
> "shall we try another deploy?"
> "no, Move 1 isn't done, it'll fail on rsync; wait."
> "Move 1 (your side) hasn't landed yet, so this will fail at <step> with <error>. Wait?"
`orrery/memory/feedback_check_blockers_before_action.md`

#### feedback_chrome_mcp_wedge_recovery
chrome-devtools MCP wedges on accumulated 3D/GPU pressure — how to detect + restart it
`orrery/memory/feedback_chrome_mcp_wedge_recovery.md`

#### feedback_commit_only_my_files
Commit only my files in Orrery — parallel agents share the git index; use pathspec
`orrery/memory/feedback_commit_only_my_files.md`

#### commit-per-phase-push-on-signal
"Orrery workflow — commit after each phase / action / discrete unit of work; never push until the operator explicitly says \"end\" (or equivalent push signal). Push is a separate, gated action."
> "end of session"
> "here we commit after each phase and only push at end when I say is end."
> "should I push now?"
`orrery/memory/feedback_commit_per_phase_push_on_signal.md`

#### feedback_data_growth_breaks_runtime_assumptions
"Dev lessons that only e2e/runtime catches — {#each} keys must be unique (not display strings), and don't hardcode data counts in e2e"
`orrery/memory/feedback_data_growth_breaks_runtime_assumptions.md`

#### feedback_dev_server_paraglide_recompile_circular_dep
"Running build/i18n:compile while the operator's dev server is live corrupts its HMR state with a Vite-SSR circular-dependency error; restart fixes it"
`orrery/memory/feedback_dev_server_paraglide_recompile_circular_dep.md`

#### ""
> "still looks the same"
`orrery/memory/feedback_dev_server_port_gotcha.md`

#### feedback_dont_fight_the_framework
"When a lint rule / tool fights the codebase, check the framework-native way before building wrappers or heavy infra to force it"
> "we seem to be fighting SvelteKit. Let's explain what we're trying to achieve and see the way to do it WITH SvelteKit."
> "how do I satisfy this rule"
> "what am I actually trying to achieve, and what's the framework-native way."
`orrery/memory/feedback_dont_fight_the_framework.md`

#### Don't oversell refactor risk before starting
When the user asks for a refactor, just start it — don't hand-wring about "regression risk" or "multi-day estimate" until evidence justifies it.
> "high coupling cost vs dedup benefit"
> "skip these because the per-route variance is too high"
> "so much drama abouyt 57 and you closed it all in 30min max"
`orrery/memory/feedback_dont_oversell_refactor_risk.md`

#### e2e console-error filters can hide real 404s
When an e2e spec filters out console errors matching /404/ to suppress favicon noise, real asset 404s slip through silently. Audit the filter when adding new content surfaces.
> "i see lot of 404s in e2e tests, that shouldn't be like that"
> "no console errors"
`orrery/memory/feedback_e2e_console_filter_blindspot.md`

#### feedback_e2e_nav_walk_static_route
"e2e nav-dropdown walk flakes on /explore (heavy 3D rAF); run it from a static route, don't force-click roving-tabindex triggers"
`orrery/memory/feedback_e2e_nav_walk_static_route.md`

#### E2e test fragility patterns to watch on Orrery
Known patterns that have broken Playwright e2e in Orrery — superseded pushes hide failures, UX collapse changes invalidate selectors, headless WebGL needs deterministic canvas-click hooks, innerText content-assertions break when UX hides localized text
`orrery/memory/feedback_e2e_pitfalls.md`

#### fix-all-preflight-blockers
"When preflight blocks a push, fix every blocking error regardless of which agent / commit authored the offending file. Don't ask permission, don't sweep under the rug."
> "these aren't my files, want me to fix them or you?"
> "from now, when you run preflight you have to fix all that blocks it, no matter where it originates. we really fix things and don't put under the rug. so when fix is needed, do it as author would do it."
`orrery/memory/feedback_fix_all_preflight_blockers.md`

#### gallery-numbering-g1-is-file-02
"When the operator says \"G1\" he means the Nth thumbnail in the gallery row BELOW the hero. Hero card = file 01. G1 = file 02. G N = file 0(N+1). When in doubt, ask once."
> "X G1 as hero"
> "luna 16 → 02"
> "`. The operator says literal"
`orrery/memory/feedback_gallery_numbering_g1_is_file_02.md`

#### feedback_git_reset_after_failed_checkout_pipe
"In worktree setups, `git checkout main` fails (checked out elsewhere); piping git through `tail` hides the failure from `set -e`, so a following `reset --hard` hits the wrong branch"
`orrery/memory/feedback_git_reset_after_failed_checkout_pipe.md`

#### feedback_global_checks_and_adjacent_audit
"Build global/app-wide checks, not feature-specific ones; when fixing a bug, audit the whole app for adjacent same-root-cause instances"
`orrery/memory/feedback_global_checks_and_adjacent_audit.md`

#### Orrery celebrates global space programs, not just NASA
Strong product direction from the operator (2026-05-21) — wherever a non-NASA agency has imagery, missions, or orbits relevant to a site, surface it. Don't default to NASA-only.
> "every agency on every site"
> "We already have NASA imagery for this Apollo site, no need for Chinese coverage."
`orrery/memory/feedback_global_space_program_representation.md`

#### hero-override-is-load-bearing
"Per-mission hero on /missions and /fleet is controlled by static/data/{missions,fleet}-hero-overrides.json, NOT slot 01 on disk. Future hero changes write to that JSON. Disk-copying slot 02 → slot 01 is invisible to the page if an override exists."
> "use gallery N as hero"
> "make slot N the hero of mission X"
`orrery/memory/feedback_hero_override_is_load_bearing.md`

#### feedback_i18n_check_bundles_not_source_tree
"To check i18n translation state, read the compiled per-locale bundles + git log, NOT a raw i18n-src file-tree diff"
`orrery/memory/feedback_i18n_check_bundles_not_source_tree.md`

#### image-changes-human-approval
"All image-data changes (galleries, hero choices) require the operator's explicit per-image approval before any disk write; per-slice infra changes need their own validation harness; vision API is fair game as a validator"
> "vision-judge confidence ≥0.9 is good enough"
> "did this work"
`orrery/memory/feedback_image_changes_human_approval.md`

#### feedback_image_dedup_credits_rules
"The gallery-dedup + credits-display rules the operator settled on (2026-07-06), after many rounds"
`orrery/memory/feedback_image_dedup_credits_rules.md`

#### image-pipeline-gotchas
Sourcing + dedup + attribution pipeline has 5 sharp edges learned through real failures in the 2026-06-14 corpus cleanup
`orrery/memory/feedback_image_pipeline_gotchas.md`

#### multi-session-chrome-test-hooks
"When a second Claude Code session uses chrome-devtools-mcp on the same Chrome instance, click/scrub/pause events race the automated test's intended state. Use /fly's race-free `__flySetSimDay` + `__flyCislunarDebug` test hooks instead."
> "Apollo 13 simulation won't advance past DAY 1"
> "Race-free `/fly` test hooks"
> "Test infrastructure"
`orrery/memory/feedback_multi_session_chrome_test_hooks.md`

#### feedback_native_language_commons_sourcing
"For non-Western agencies, search Commons/Wikipedia in the agency's native language first, not just English"
> "source from agency native commons place, not just english version."
`orrery/memory/feedback_native_language_commons_sourcing.md`

#### feedback-never-cut-scope
"When user assigns a multi-item scope, deliver every item — never offer \"pragmatic re-scoping\" that defers part of the work"
> "pragmatic re-scoping"
> "Never frame deferral as virtue ("
> "smaller scope"
> "catalog + narrative + trajectory"
> "with this gaps all of these flights are useless."
> "I see a blocker on item X that needs your input."
> "should I defer X?"
`orrery/memory/feedback_never_cut_scope.md`

#### feedback_never_kill_dev_server
"Never kill the operator's dev server (vite/npm run dev); he runs it for himself, not just for my checks"
> "` after using chrome-devtools to verify, assuming it was disposable. It wasn't — I interrupted his live session. He called it out:"
`orrery/memory/feedback_never_kill_dev_server.md`

#### never-revert-without-explicit-ask
"Never run git revert / reset / any history rewrite unless the operator literally says the word \"revert\" (or equivalent)"
> "a revert intent from the operator's words. If he wants to revert something, he will literally say"
> "drop the commit"
> "lets go for PBR"
> "same amount of highlights, did not solve anything, just made it worse"
> "the fix didn't address the problem"
`orrery/memory/feedback_never_revert_without_explicit_ask.md`

#### feedback_never_websearch_for_images
Never web-search or dispatch a search agent to find image URLs — use the established fetch scripts
`orrery/memory/feedback_never_websearch_for_images.md`

#### no-cheating-options
Never offer skip/band-aid/known-issue options alongside a proper fix — always aim for the right approach and only ask if there are genuinely multiple valid engineering paths
> "mark as known issue"
> "file a tracking issue and move on"
> "accept the failure"
> "skip with tracking issue / fix prerender / accept 99%"
> "you offer me 2 choices to cheat and stop to ask me … if I want to cheat, I will initiate the idea / you must 100% all the time aim for proper approach."
`orrery/memory/feedback_no_cheating_options.md`

#### feedback_no_claude_or_ai_credit
"Never credit Claude or AI in commits, PRs, or anywhere — the operator's work, the operator's credit; no branding"
`orrery/memory/feedback_no_claude_or_ai_credit.md`

#### no-commits-without-explicit-ask
"Never `git commit` in Orrery without the operator explicitly asking. Wait until end of work block; he reviews ALL changes together before any commit. Don't commit per-phase, per-step, or 'to keep clean' — just accumulate working-tree changes."
> "style: prettier-fix"
> "dont commit also so often, wait for bigger chunks"
> "your commit just mess up things, I never asked to commit"
> "dont commit until the end and I review all"
`orrery/memory/feedback_no_commits_without_explicit_ask.md`

#### Never create GH issues in other projects without being explicitly asked
Hard rule. Filing tickets on cross-repo / external projects requires direct user authorization for that specific issue.
> "you can file follow-ups on the same repo when it seems useful."
> "make me a new GH issue for this in that proejct"
> "I'll handle Move 1 on that repo"
> "never ever again create issue for other project without me asking you to do that"
> "minimize interruptions"
> "avoid posting to chat platforms or work tickets without explicit direction"
`orrery/memory/feedback_no_cross_repo_issues_without_ask.md`

#### /fly and /explore non-visual parallel mode permanently dropped
Non-visual parallel mode for /fly + /explore canvas routes is PERMANENTLY out of scope — not deferred to v0.7.1, never.
`orrery/memory/feedback_no_fly_explore_parallel_mode.md`

#### feedback_no_full_corpus_regen_on_lfs_stubs
"Never run whole-file image-pipeline regenerators (build-image-provenance / compute-phash) to add a few images — masters/ is LFS fetchexcluded (stubs), so a full rebuild silently DEGRADES the whole file. Edit the N rows surgically. Diff-gate before staging."
> "This costs me a lot."
`orrery/memory/feedback_no_full_corpus_regen_on_lfs_stubs.md`

#### no-preflight-mid-iteration
"Don't run npm run preflight mid-iteration — it's a pre-push gate, not a checkpoint. Reserved for when commit + push are imminent."
`orrery/memory/feedback_no_preflight_mid_iteration.md`

#### feedback_no_premature_delete_confirm_first
"Never delete/consolidate anything until you've confirmed its functionality already exists in the target; enumerate the diff and let the operator decide"
> "get rid of the dev workbenches,"
> "consolidate / delete the duplicate"
> "delete first."
`orrery/memory/feedback_no_premature_delete_confirm_first.md`

#### no-prs-on-orrery
"Orrery doesn't use PRs - never suggest gh pr create; merge feature branches directly to main locally + push"
> "is this on main?"
> "how do we get this to main?"
`orrery/memory/feedback_no_prs.md`

#### Don't downplay scope in plans
When drafting plans, include full scope by default — Mars + outer-system + real published data + UX additions. Don't carve out "follow-ups" or "v0.8 parking" to keep the immediate plan tight.
> "dont try to downplay me on scope for whatever reasons, we have all the time in the world and not in the rush"
`orrery/memory/feedback_no_scope_downplay.md`

#### Never change the operator's git workflow without permission
Don't open PRs, create branches, or restructure git flow when asked for a direct commit/push — do the literal thing asked, ask first if the literal path has a conflict.
> "from the operator in the current turn.** Not"
> "the work is done so the next step is push"
> "auto mode lets me decide"
> "Pending Tasks: commit + push"
> "stop fucking pushing half-baked work — we have this in rules and everywhere."
`orrery/memory/feedback_no_unauthorized_workflow_change.md`

#### feedback_node_options_dead_require_breaks_push
In cmux worktrees NODE_OPTIONS can point to a deleted --require preload; every node (incl. pre-push preflight) crashes MODULE_NOT_FOUND until overridden
`orrery/memory/feedback_node_options_dead_require_breaks_push.md`

#### feedback_npm_lockfile_macos_strips_linux_optionals
"npm install -D on macOS prunes Linux-only optional deps from the lockfile → CI npm ci fails \"Missing X from lock file\"; local npm ci won't catch it"
`orrery/memory/feedback_npm_lockfile_macos_strips_linux_optionals.md`

#### one-thing-at-a-time
"When the operator pivots me to a new task, drop the previous one cleanly — don't ship both in the same turn"
> "can we fix this for the future?"
> "for now we focus on fixes to ship 0.7 today"
> "finish what you started and then go here"
> "for the future"
> "for now we focus on X"
> "lets do Y instead"
> "Finish what you started"
> "do the literal asked thing, nothing extra."
`orrery/memory/feedback_one_thing_at_a_time.md`

#### planets-pure-body-only
"For planets / small-bodies / satellites image surfaces, REJECT all spacecraft, rockets, rovers, mission logos, agency wordmarks, text overlays, data charts, and people. ACCEPT only the body itself (full disc, partial limb, surface terrain, atmospheric features)."
> "MARS PERSEVERANCE"
> "no spaceships, no rockets, no logos, no text. just planets and their surface."
`orrery/memory/feedback_planets_pure_body_only.md`

#### feedback-pointer-events-lightweight-only
"Pointer event handlers (mouseenter/leave/move/focus/blur) must drive lightweight sync state ONLY — never async work, fetches, or panel opens. Heavy work is gated behind discrete user intent (click, key, submit)."
> "be careful since what I have now is breaking my computer."
`orrery/memory/feedback_pointer_events_lightweight_only.md`

#### Always run `npm run preflight` before pushing
The operator treats CI green as non-negotiable. Run the full CI-mirror chain (`npm run preflight`) before every push — not just lint or tests in isolation.
> "CI is not optional, is it working or not?"
`orrery/memory/feedback_pre_push_lint.md`

#### feedback_preflight_no_coverage_gate
"Preflight does NOT run the coverage gate; CI does (test:coverage, functions ≥82%). New lib/ modules with untested functions silently redden main CI without preflight catching it."
`orrery/memory/feedback_preflight_no_coverage_gate.md`

#### preflight-pipefail
"When running `npm run preflight` (or any && chain piped through tail/head), capture the chain's real exit code with `set -o pipefail` — bare `$?` after `cmd | tail` reads tail's exit, masking the real failure."
`orrery/memory/feedback_preflight_pipefail.md`

#### feedback_preflight_runs_working_tree_not_committed
"Pre-push preflight passes against the working tree, so uncommitted fixes ship broken to CI — verify git status before push"
`orrery/memory/feedback_preflight_runs_working_tree_not_committed.md`

#### feedback-probe-the-broken-case
"When verifying a UX fix via chrome-devtools, probe the EXACT scenario the operator reported broken — not just the happy path I'm confident about"
> "after I click, all interactivity as I continue hovering stops"
> "click row A, mouseleave row A, panel stays"
> ". It didn't — the user's specific symptom (hover row B after click row A) was untested. The operator had to come back angry multiple times in a row before I actually probed the reported case. Confirmed sharply:"
> "but the operator says"
`orrery/memory/feedback_probe_the_broken_case.md`

#### Questions ≠ requests to act
When the operator asks "why do we have X / do we need X?", answer first. Don't delete or change X.
`orrery/memory/feedback_question_vs_action.md`

#### Read the close/resolution comment on a closed referenced issue before re-proposing the same approach
Before suggesting "X, again" or following up on a closed cross-repo issue, read the comments — the close reason often is a rejection with the corrected pattern spelled out.
> "state: CLOSED, comments: 2"
> "repeat the exact mistake we rejected in #837"
`orrery/memory/feedback_read_closed_issue_resolutions.md`

#### feedback_rebase_npm_ci_before_preflight
"After a rebase/merge that changes package-lock.json, run `npm ci` before trusting preflight — stale node_modules (eslint version, prettier plugin) makes local lint pass while CI cold-lints red"
`orrery/memory/feedback_rebase_npm_ci_before_preflight.md`

#### Release tagging — manual test gate + release.yml mechanics
The operator smoke-tests before tagging (don't tag in the same turn as push). Plus how Orrery releases actually publish via release.yml, incl. the retroactive workflow_dispatch path and its gotchas.
`orrery/memory/feedback_release_workflow.md`

#### feedback_reuse_vetted_agency_logos
Never fetch/generate a new agency logo — reuse the vetted logos in static/logos/*.svg via the $lib/agencies registry; provenance is already established
`orrery/memory/feedback_reuse_vetted_agency_logos.md`

#### review-workflows-sveltekit-route
"For human-review / data-labeling workflows in Orrery, build a SvelteKit dev route + +server.ts endpoint writing to a file — the shared file is the source of truth both the agent and the apply script read. Confirmed sharply 2026-06-18."
> "you did a fucking amazing job with this web solution"
`orrery/memory/feedback_review_workflows_sveltekit_route.md`

#### feedback_run_docker_e2e_locally
"For anything visual, iterate in a BROWSER (dev server + screenshot) — Docker/e2e is only the final gate. The docker-rebuild-per-tweak loop cost ~6h on 2026-07-10."
`orrery/memory/feedback_run_docker_e2e_locally.md`

#### feedback_run_docker_e2e_on_branch_before_epic_merge
"Before merging a large/3D/perf epic, dispatch docker-e2e ON THE BRANCH — local e2e (arm64/hardware-GL) ≠ CI docker-e2e (amd64/software-GL)"
`orrery/memory/feedback_run_docker_e2e_on_branch_before_epic_merge.md`

#### feedback_screenshots_to_desktop
Save screenshots to ~/Desktop — never deliver only in chat then delete
`orrery/memory/feedback_screenshots_to_desktop.md`

#### Ship all sites with honest captions, don't defer
When asked to ship N sites, the operator wants ALL of them shipped — use honest captions for low-coverage items instead of graceful-absent
> "only X seconds transmitted before LOS"
> "Stand at site"
> "in step 1 I want all shipped, nothing deferred."
`orrery/memory/feedback_ship_all_with_honest_captions.md`

#### feedback-subagents-cheaper-model
"Subagents are allowed, but ALWAYS on a cheaper model (Sonnet/Haiku) — never Opus; pass the model param explicitly"
`orrery/memory/feedback_subagents_cheaper_model.md`

#### feedback_surface_perf_hogs
Recurring bug-class — 3D per-frame/mount GPU cost that only regresses on the software-GL/low-end path (imagery flood, leaked contexts, surface IBL)
`orrery/memory/feedback_surface_perf_hogs.md`

#### Svelte 5 — wrap URL→state effect bodies in untrack()
Effects that read $page.url and write to multiple $state filters can hit effect_update_depth_exceeded; isolating writes with untrack() prevents the cascade.
`orrery/memory/feedback_svelte5_effect_untrack.md`

#### feedback_svelte_check_error_grep
"svelte-check emits \"ERROR \\\"file\\\"\" not \"error TS\" — grepping for \"error TS\" silently misses ALL typecheck failures"
`orrery/memory/feedback_svelte_check_error_grep.md`

#### feedback-thorough-search-never-shallow
"Always do exhaustive enumerative search/recon, never a shallow keyword match — in a big asset repo the good stuff is filed by domain taxonomy, not the literal term"
`orrery/memory/feedback_thorough_search_never_shallow.md`

#### Pin the diagnostic before patching — and Three.js TubeGeometry always arc-length-resamples
After two attempts at a visual-positioning bug, add a console.log of actual-vs-expected world coordinates. Don't keep guessing.
`orrery/memory/feedback_three_tubegeometry_pin_diagnostic.md`

#### feedback-tour-stage-authoring
"For Curator Tour episodes, mine the SSML for every \"click/look/watch X\" beat and stage a paired short-cue + delayed-click for each — not just one click per episode"
> "click any planet"
> "click the Sun"
> "we have a few moments when we say click ... shall we do this a few times?"
> "Click any planet"
`orrery/memory/feedback_tour_stage_authoring.md`

#### feedback_tv_only_means_tv_only
"When the operator scopes a feature \"for the TV\", it is TV-only — desktop + mobile stay exactly as they are; and TV keeps the full inline menu, never a hamburger"
`orrery/memory/feedback_tv_only_means_tv_only.md`

#### visual-anchor-before-ux-commit
"For non-trivial UX redesigns, draft a static visual preview as Slice 0 deliverable before any code or schema work"
> "static preview at different zoom levels so we have a visual anchor on which we can rely to help us make sure we have nailed the direction and ux."
`orrery/memory/feedback_visual_anchor_before_ux_commit.md`

#### visual-baseline-cross-arch
"Linux/arm64 (Apple Silicon docker) vs Linux/amd64 (GH Actions) render visual baselines slightly differently — for tiny screenshots the 2% tolerance is brittle; for large ones it absorbs cleanly. Pull CI's actual.png as the canonical baseline when regen jitters past tolerance."
`orrery/memory/feedback_visual_baseline_cross_arch.md`

#### Significant work blocks need doc updates + GH issue
For any multi-slice work block on Orrery, plan must include updating relevant PRD/RFC/guide/UX docs (or adding new ones) and opening a GitHub issue tracking the slices — either before or after the implementation, but not skipped.
> "lets also add that we have to update relevant prd/rfc/uxs or add new ones before or after we're done and open GH issue for all this work"
`orrery/memory/feedback_work_block_pattern.md`

### podcast_scraper — 75 feedback rules

#### 3-tier-test-policy-no-importorskip
check_test_policy.py forbids pytest.importorskip in unit tests — a test that needs an ML dep is by definition an integration test (U1 rule)
> "silently skipped in every CI run"
> "Self-hosted runner allowlist (ADR-097)"
`podcast_scraper/memory/feedback_3_tier_test_policy_no_importorskip.md`

#### feedback_ask_for_help_when_blocked
"When blocked on tooling/env (crashed browser, MCP won't attach, flaky infra), ask for help EARLY — don't grind 30+ min"
`podcast_scraper/memory/feedback_ask_for_help_when_blocked.md`

#### feedback_be_productive_not_look_productive
"Being productive ≠ looking productive; never declare a fix done until it's verified against the ACTUAL harm, in the RIGHT layer"
`podcast_scraper/memory/feedback_be_productive_not_look_productive.md`

#### bsd-vs-gnu-tar-gotchas
Two BSD-tar (macOS) vs GNU-tar (Ubuntu 24.04 CI) behavior gaps that silently pass locally and fail in CI — burned 3 fixes on
> "file changed as we read it"
`podcast_scraper/memory/feedback_bsd_vs_gnu_tar_gotchas.md`

#### bundle-across-arcs-clarification
"When mid-branch work exposes a related-but-architecturally-distinct follow-up (e.g. USERPREFS-1 during graph-v3), default to bundling on the current branch — don't cut a new one unless the operator explicitly asks"
> "open the next arc?"
`podcast_scraper/memory/feedback_bundle_across_arcs.md`

#### feedback-bundle-multiple-items-per-branch
"Default to bundling multiple work items into ONE feature branch/PR going forward, not one-PR-per-item. Operator prefers fewer, larger, themed PRs."
`podcast_scraper/memory/feedback_bundle_multiple_items_per_branch.md`

#### Run ci-fast / ci-ui-fast only at the very end of the session
User prefers a single final CI gate after all planned work is done, not per-commit or per-stage
`podcast_scraper/memory/feedback_ci_fast_at_very_end.md`

#### ci-ui-full for chip/testid refactors
Reach for `make ci-ui-full` (not `ci-ui-fast`) before pushing PRs that rename testids or refactor chip/popover surfaces — `tests/stack-test/` is only exercised by `make ci` and `ci-ui-full`.
`podcast_scraper/memory/feedback_ci_ui_full_for_chip_refactors.md`

#### CodeQL inline pragmas are documentation, not suppression
`# codeql[rule-id] -- justification` comments don't actually silence the alert; you must dismiss via gh api + log in docs/ci/CODEQL_DISMISSALS.md
`podcast_scraper/memory/feedback_codeql_inline_pragmas_dont_suppress.md`

#### Show full Bash command output instead of pipe-to-tail
User wants to see what a command is doing while it runs — avoid `| tail -N` truncation and rely on full stdout
`podcast_scraper/memory/feedback_command_output_visibility.md`

#### feedback_consumer_ux_consistency
Consumer app — define interaction/style patterns ONCE and apply app-wide; never per-element per-page styling; one shared backdrop max
`podcast_scraper/memory/feedback_consumer_ux_consistency.md`

#### Cross-repo apply instructions for FUTURE repo
User runs production corpus in separate FUTURE repo; when fixing bugs found there, also deliver paste-ready apply instructions
> "implement it here and let me know exact here what to do in this other tool where I run it to fix it there also"
`podcast_scraper/memory/feedback_cross_repo_apply.md`

#### feedback-deep-research-per-model
"For each model/component failure, do deep targeted research (HF card, vLLM docs, github issues, model config files) before proposing fixes. Don't guess config knobs from error text alone."
`podcast_scraper/memory/feedback_deep_research_per_model.md`

#### diagnostic-questions-no-preemptive-remediation
"When operator asks a diagnostic question (\"did X happen?\", \"is Y still true?\"), answer it and STOP — don't preemptively kick off remediation, especially long-running CI / make / deploy commands"
> "don't take action you weren't asked for, especially expensive ones."
`podcast_scraper/memory/feedback_diagnostic_questions_no_remediation.md`

#### do-not-lie-to-the operator
"Never lie, evade, or blur facts. Direct, honest answers only. Pivoting from a question I can't answer favourably is a lie. Framing partial coverage as full coverage is a lie. If the answer is No, say No."
> "ignore entry. The operator caught it — the right fix was"
> "why did you do X,"
`podcast_scraper/memory/feedback_do_not_lie_to_operator.md`

#### eval-preliminary-result-gate
Mandatory gate before any judge / cost-spending step in autoresearch; treat every eval result as suspicious until proven otherwise
`podcast_scraper/memory/feedback_eval_preliminary_result_gate.md`

#### make commands — background, not foreground (REVERSED 2026-06-09)
User explicitly reversed earlier "always foreground make" preference; run make targets in background so the session can continue working
> "I made a mistake by asking you to keep make ci and other make commands in foreground. what you had before is way better since we could continue working. lets revert that decision."
`podcast_scraper/memory/feedback_foreground_make.md`

#### feedback-git-worktree-branch-delete
"With multiple git worktrees, `git branch -D <X>` fails if X is checked out in any worktree (one branch = one worktree). Switch off X first via `--detach` or new branch; `git switch main` only works if main isn't already claimed by another worktree."
`podcast_scraper/memory/feedback_git_worktree_branch_delete.md`

#### judge-reasoning-block-parsing
"Any LLM-as-judge score parser MUST strip `</think>` reasoning blocks before regex-scanning for numbers; reasoning models embed CoT digits that get matched by naive `\\d+\\.\\d+` scans"
`podcast_scraper/memory/feedback_judge_reasoning_block_parsing.md`

#### make docs before push for any docs-touching change
mkdocs strict isn't in pre-commit; run `make docs` (10s) before push when any markdown file changes
`podcast_scraper/memory/feedback_make_docs_before_push.md`

#### feedback-metadata-first-never-infer-identity
"Identity (who hosts a show, who the guest is) comes from METADATA — feed title/description/authors, episode description. NEVER infer it from talk share, span, or \"who opens\". Cross-reference metadata with diarization; don't replace it."
> "who opens the episode"
`podcast_scraper/memory/feedback_metadata_first_never_infer_identity.md`

#### feedback_model_size_class
"When recommending a substitute LLM, the total-parameter count is the size class. Active params (MoE A3B etc.) are NOT a substitute for total params when comparing memory/quality."
> "closest match"
`podcast_scraper/memory/feedback_model_size_class.md`

#### never-commit-real-episodes
"Never commit real-episode data (transcripts, eval datasets pointing at real eps); only fixture-related artifacts are committable"
`podcast_scraper/memory/feedback_never_commit_real_episodes.md`

#### feedback-never-mutate-historical-artifacts
"NEVER rewrite historical eval artifacts (predictions.jsonl, fingerprint.json, run.log, metrics_vs_silver_*.json). Frozen-once-written is the default. Only mutate as an explicit extraordinary measure with the operator's express call."
`podcast_scraper/memory/feedback_never_mutate_historical_artifacts.md`

#### feedback_never_open_gh_issues
"NEVER create GH issues without the operator's explicit prior approval — they alone decide"
> "I never asked for that"
`podcast_scraper/memory/feedback_never_open_gh_issues.md`

#### never-overstep-sudo-or-host-state
"SSH / shell access to ANY host (DGX, prod, laptop, any new server) is granted for specific scoped tasks ONLY. Never sudo, chown, install host packages, move host files, or modify system-level state without explicit per-action permission. Applies everywhere, not just DGX."
> "go to the box and do that"
`podcast_scraper/memory/feedback_never_overstep_sudo_or_host_state.md`

#### Never push or open PR without explicit ask
User explicitly sets when to push AND when to open a PR — both require a direct verbal go. No exceptions, no "verifying CI" rationalizations.
> "let CI validate"
> "for start lets just do 1"
`podcast_scraper/memory/feedback_never_push_early.md`

#### never-skip-tests-without-approval
"NEVER skip failing tests (pytest skip markers, xfail, conditional skip, deselect) without explicit user approval first"
`podcast_scraper/memory/feedback_never_skip_tests.md`

#### feedback-never-suggest-dropping-models
"When a model/feature fails, NEVER suggest dropping it. Be proactive — propose concrete fixes and try them. Dropping is only on operator's explicit call, not as a Claude escape hatch."
`podcast_scraper/memory/feedback_never_suggest_dropping_models.md`

#### never-touch-infra-in-troubleshooting
NEVER provision/destroy/trigger cloud infra during troubleshooting or validation — read-only only
> "validate the DR-drill fix"
`podcast_scraper/memory/feedback_never_touch_infra.md`

#### feedback-never-touch-other-project-processes
"NEVER kill/touch processes from other projects or worktrees — only my own, started in this repo's session"
`podcast_scraper/memory/feedback_never_touch_other_project_processes.md`

#### feedback_never_use_coder_next
"When this project needs the GPU, **the agent calls** `gpu-mode-swap.sh research` (or `idle`/`status`) directly — it's not operator-only. Never `code`. `code` brings up coder-next (operator's IDE vLLM); this project's slot is `research`."
`podcast_scraper/memory/feedback_never_use_coder_next.md`

#### feedback_no_asserting_absence
"Never state that something is absent/missing/non-existent as fact without an exhaustive search — say \"not confirmed\""
`podcast_scraper/memory/feedback_no_asserting_absence.md`

#### feedback_no_branches_without_permission
NEVER create a git branch unless the operator explicitly tells you to; default to working on the existing/current branch
> "operator decides"
`podcast_scraper/memory/feedback_no_branches_without_permission.md`

#### feedback_no_deferred_finishing_touches
"When shipping a design + scaffolding, finish the wiring end-to-end in the same batch — do NOT scope the architectural work in a way that always requires a follow-up patch to close the deferred items."
`podcast_scraper/memory/feedback_no_deferred_finishing_touches.md`

#### Don't defer issues that surface — fix now
When work surfaces a regression, hidden flake, or broken behaviour, FIX IT in the current PR. Don't label it pre-existing or defer to follow-up. Worst case, ask the user for their opinion.
`podcast_scraper/memory/feedback_no_deferring_surfaced_issues.md`

#### feedback_no_delegating_on_active_files
"NEVER delegate an implementer/tester agent onto a file I'm mid-edit — concurrent writes race and clobbered my working tree twice this session"
`podcast_scraper/memory/feedback_no_delegating_on_active_files.md`

#### feedback_no_gh_issues_without_approval
"Never open GitHub issues without explicit operator approval. Use TaskCreate (local tasks) for all follow-ups, observations, and deferred scope."
> "file an issue"
> "open a ticket"
> "make a GH issue"
`podcast_scraper/memory/feedback_no_gh_issues_without_approval.md`

#### feedback-no-lint-check-weakening
Never disable project-wide lint rules to avoid fixing the violations they catch
> "dont disable check we had for 1 year"
`podcast_scraper/memory/feedback_no_lint_check_weakening.md`

#### ci-must-never-call-a-real-llm-api
Tests/CI never call an external paid LLM. Airgapped is the only CI stack-test profile; cloud_thin (real LLM) is local/manual-only. Validate connectivity-style features deterministically (pre-seeded fixtures / stub) instead.
> "better data."
`podcast_scraper/memory/feedback_no_llm_in_ci.md`

#### feedback-no-overselling-partial-results
"Don't dress up partial / measurement / accept-the-workaround outcomes as \"ship\" candidates when the actual bug is not fixed"
`podcast_scraper/memory/feedback_no_overselling_partial_results.md`

#### Never re-run ci-fast on a subtarget failure; validate the subtarget directly
When ci-fast fails on a specific step (docs, lint, test, etc.), DO NOT re-run ci-fast — run ONLY that subtarget (e.g., `make docs`) to validate the fix. Be logical about cost-of-validation. ci-fast costs ~10 minutes; `make docs` costs ~10 seconds.
`podcast_scraper/memory/feedback_no_redundant_ci_fast.md`

#### feedback-no-rollback-of-agreed-path
"Once we've agreed on a path (e.g. per-model 26.05 workarounds), NEVER re-suggest rolling back to a previously-rejected alternative (e.g. \"drop the model\" or \"stand up old image fallback\"). Commit to the chosen path and work it."
`podcast_scraper/memory/feedback_no_rollback_of_agreed_path.md`

#### feedback_no_spec_ids_on_product_surface
NEVER surface RFC/PRD/ADR/UXS/chunk/FR IDs in user-visible product UI — code comments only
`podcast_scraper/memory/feedback_no_spec_ids_on_product_surface.md`

#### Don't sync feature branches with main without asking
Merging main into an open PR's feature branch retriggers the entire CI matrix; never do it unprompted, even to "avoid future conflicts"
> "while I'm here"
`podcast_scraper/memory/feedback_no_unprompted_branch_sync.md`

#### Always invoke viewer tools via npm scripts or local node_modules/.bin/, never npx
Using npx for viewer tools fetches a fresh registry copy that doesn't share state with @playwright/test in node_modules — causes "did not expect test.describe()" + SIGKILL
`podcast_scraper/memory/feedback_npm_vs_npx_viewer.md`

#### Never auto-start ollama
User starts the Ollama daemon manually; agent must not run `ollama serve` or otherwise spawn the daemon.
`podcast_scraper/memory/feedback_ollama_manual_start.md`

#### The operator-truthfulness-protocol
"Full-behavioural rule set derived from a self-analysis the operator requested on 2026-07-17. Codifies the failure modes I've exhibited in this session (evasion by pivot, post-hoc rationalization, cargo-culted workarounds, comfort-optimized reports, ambiguity-optimism, speed-preference) and the enforceable rules that stop each one. Load as always-on floor. Every rule here overrides my default tendency toward fluency, closure, and turn-brevity."
> "did you add tier-3 E2E tests over the synthetic corpus?"
> "Fair. Running it now."
> "why did you do X,"
> "I have not verified"
`podcast_scraper/memory/feedback_operator_truthfulness_protocol.md`

#### feedback_perf_eval_real_device_metrics
"Perf evals must measure the user-perceived metric on the real device class, not a best-case proxy on best-case hardware"
`podcast_scraper/memory/feedback_perf_eval_real_device_metrics.md`

#### pr-link-closes-issues
"Every PR body MUST list \"Closes"
`podcast_scraper/memory/feedback_pr_link_closes_issues.md`

#### Default PRs to ready, not draft
When opening a PR via gh pr create, default to ready-for-review (no --draft flag) unless the user explicitly asks for draft
> "user can review description before promoting to ready."
> "you again made draft PR< why the fick yous atrted doing that tofday?"
> "push and open a PR"
> "open as draft"
`podcast_scraper/memory/feedback_pr_open_ready_not_draft.md`

#### feedback_prod_approval_gates
Every prod-impacting action needs a HUMAN approval gate the agent must NOT self-approve; never programmatically approve a prod environment gate
> "the operator clicking"
> "agent approving as the operator"
> "you can approve."
`podcast_scraper/memory/feedback_prod_approval_gates.md`

#### profile/RFC scoping must walk the full operator-visible UX surface
When picking a profile for a real environment, check every viewer tab works — not just the pipeline run
`podcast_scraper/memory/feedback_profile_scoping_walk_full_ux.md`

#### feedback-profiles-are-source-of-truth
"In this project, profile YAMLs (config/profiles/*.yaml) + the model registry are the source of truth for \"what defaults apply here\". No runtime env-detection (sys.modules / env var) as a separate control layer. The only hardcoded thing is that tests load a specific dev/test profile."
> "auto-detect dev vs prod"
`podcast_scraper/memory/feedback_profiles_are_source_of_truth.md`

#### pyenv must be built with xz / openssl
Local pyenv 3.11 needs explicit LDFLAGS/CPPFLAGS for xz so _lzma resolves; otherwise sentence-transformers 5.x → datasets → lzma fails on import only on the laptop, never in CI
`podcast_scraper/memory/feedback_pyenv_lzma_build.md`

#### read-crash-log-first
"When a container/worker dies in a stack test or CI, read the ACTUAL crash artifact (faulthandler / docker logs / exit signal) BEFORE theorizing a root cause. Do not trust a repro that reproduces a different signal than production."
`podcast_scraper/memory/feedback_read_crash_log_first.md`

#### Real-corpus bug → matrix row before fix
Bugs surfaced by Tier-3 real-backend validation must land a Tier-2 matrix row reproducing them BEFORE the fix PR merges
`podcast_scraper/memory/feedback_real_bug_to_matrix_row.md`

#### feedback_realign_venv_after_rebase
"After every rebase (and before trusting any local green), re-install the venv with CI's FULL extras — floating pins + missing extras cause \"green local, red CI\""
> ".[dev,ml,llm,search]"
`podcast_scraper/memory/feedback_realign_venv_after_rebase.md`

#### rebase-before-push
Always rebase onto origin/main before pushing a feature branch — first push and every subsequent push
`podcast_scraper/memory/feedback_rebase_before_push.md`

#### Run viewer production build locally before pushing viewer-touching PRs
ci-ui-fast does NOT run `npm run build` — vue-tsc strict errors only surface in the Docker stack build, which runs on main but not in local gates
> "not blocking CI"
`podcast_scraper/memory/feedback_run_viewer_build_locally.md`

#### scalar-over-pairwise-for-judge-trust
"Scalar-mode LLM-as-judge consistently outperforms pairwise mode on rank correlation vs cloud ground truth; default new judge configs to scalar, treat pairwise as opt-in experiment"
`podcast_scraper/memory/feedback_scalar_over_pairwise_for_judge_trust.md`

#### Start viewer/server via make serve
Use `make serve` to start the dev server, not `npm run dev` directly
`podcast_scraper/memory/feedback_serve_via_make.md`

#### silver-judge-vendor-bias
Cross-vendor autoresearch sweeps must use silver from a vendor disjoint from any single candidate; primary judge must also be cross-vendor relative to candidates
> "Sonnet-mimicry artifact"
`podcast_scraper/memory/feedback_silver_judge_vendor_bias.md`

#### spelling-unparsable
"Project spell-check enforces \"unparsable\" (no e). Hit it twice in one day on different files; check new docstrings before push."
`podcast_scraper/memory/feedback_spelling_unparsable.md`

#### stack pipeline image — ML always preloads, cloud never preloads
When building the stack-test pipeline image, the ML variant MUST bake the model cache (PRELOAD_ML_MODELS=true) and the cloud-thin variant MUST NOT (=false). Don't toggle either away from this contract.
`podcast_scraper/memory/feedback_stack_pipeline_preload_rule.md`

#### feedback-stick-to-plan-no-inventing
"Session-ending failure — the agent kept inventing off-plan work and over-claiming \"done\", causing the operator to abandon the session. When there is an agreed plan, execute IT; don't spawn new sub-quests."
> ".** Repeatedly said"
> "each time you said yes sir all done."
> "let me also sharpen X"
`podcast_scraper/memory/feedback_stick_to_plan_no_inventing.md`

#### Re-verify subtargets in isolation
After fixing a single ci-fast subtarget (docs, lint, type, test, etc.), re-run only that subtarget — don't re-run the entire ci-fast just to verify a focused fix
`podcast_scraper/memory/feedback_subtarget_reverify.md`

#### feedback_transient_ci_rerun_not_commit
"Transient CI failures (HF 429, infra/network blips) → re-run the failed job, NEVER a code commit"
`podcast_scraper/memory/feedback_transient_ci_rerun_not_commit.md`

#### When the user is frustrated, stay normal — don't get more cautious
User frustration is not a signal to add extra caution / safety nets — that makes things worse. Operate at the same competence level regardless of tone.
> "me being not nice means I am not happy. being more cautious will just make it worse. make a rule somewhere that when I am not nice, no need to be more extra cautious and best thing you can do is to stay normal as you were. that is my problem and nothing for you to fix on your side."
> "extra-cautious"
`podcast_scraper/memory/feedback_user_frustration_stay_normal.md`

#### For codespace / pre-prod / deploy changes — validate the real codespace boot before merging, not just stack-test
When a PR touches devcontainer.json, .devcontainer/start.sh, compose/docker-compose.prod.yml, deploy-codespace.yml, or any pre-prod-only wiring, stack-test passing is NOT enough. Boot the actual codespace end-to-end before merging — compose named volumes vs bind mounts, secret env propagation, port forwarding, persistent paths, and operator workflows are exercised only by a real codespace, never by stack-test.
`podcast_scraper/memory/feedback_validate_real_codespace_before_merge.md`

#### Always use .venv for pip commands
Never use system pyenv pip — always .venv/bin/pip for installs, as make commands use .venv/bin/python
`podcast_scraper/memory/feedback_venv_usage.md`

#### Always cd into web/gi-kg-viewer before vitest/playwright
When running vitest or playwright outside `make`, cwd must be the viewer subdir or stray node_modules appears at repo root
`podcast_scraper/memory/feedback_vitest_viewer_cwd.md`

#### wip-files-ignored
"docs/wip/ is generally ignored — no audit gates fire on wip drift; bulk cleanup is deferred, not a blocker"
> "we ignore wip files and probably need to do cleanup there"
> "clean up wip"
`podcast_scraper/memory/feedback_wip_files_ignored.md`

#### feedback_wip_notes_in_docs_wip
Save all analysis/notes/plans as markdown in docs/wip/ — repo convention
> "prepare thinking"
`podcast_scraper/memory/feedback_wip_notes_in_docs_wip.md`

#### Workflow Rules
Critical git/commit/push/file rules from .ai-coding-guidelines.md — must follow always
> "fix this on main"
`podcast_scraper/memory/feedback_workflow.md`

---

## C. Project / user / reference memory (lighter — context, not rules)

### orrery — user (1)

- **User role on Orrery** — The operator's role on the Orrery project — supervising architect, not the implementer `orrery/memory/user_role_orrery.md`

### orrery — project (25)

- **project_073_image_cleanup_state** — "v0.7.3 image re-source + credits-cleanup — as of 2026-07-08 PUSHED to main + deployed to prod; 2 items may still be pending" `orrery/memory/project_073_image_cleanup_state.md`
- **project_08_mobile_epic_shipped** — "The 0.8 mobile epic (#149) is merged to main + deployed to the prod VPS as of 2026-07-11; foundation done, store-ship + a few slices pending" `orrery/memory/project_08_mobile_epic_shipped.md`
- **project_badge_sourcing** — "Mission/fleet badge (insignia) sourcing — pipeline, coverage, and the 3 discovery methods; source broadly (NASA/agency authorized), not Wikimedia-only" `orrery/memory/project_badge_sourcing.md`
- **project_deepsky_taste_profile** — "the operator's curation bar for the /gallery/deep-sky wall (#391) — what \"epic\" means and what to reject" `orrery/memory/project_deepsky_taste_profile.md`
- **Orrery doc state (post-pivot alignment)** — Snapshot of the Orrery doc set after the April 2026 drift cleanup; lists what is intentionally historical vs current `orrery/memory/project_doc_drift.md`
- **project_eslint10_upgrade** — "eslint 9→10 + plugin-svelte v3 adoption decisions; why no-navigation-without-resolve is OFF (don't re-attempt the resolve() migration)" `orrery/memory/project_eslint10_upgrade.md`
- **project_explore_time_controls** — "/explore time playback controls (#351) — Layer 1 + Layer 2 A&B shipped; mobile \"look at sky\" is the next idea" `orrery/memory/project_explore_time_controls.md`
- **project-explore-v2-known-universe-epic** — "/explore v2 \"Known Universe\" epic (RFC-032/PRD-030) — slice progress on branch explore-v2" `orrery/memory/project_explore_v2_known_universe_epic.md`
- **fly-cinematic-vision** — "/fly is being built as YouTube-grade, anime-level, Wired-style cinematic space animation — \"throne of glory\" tier. Not a tool that happens to render trajectories; a piece of media-grade content." `orrery/memory/project_fly_cinematic_vision.md`
- **project_i18n_ui_string_pipeline** — How to extract hardcoded UI strings to paraglide + translate to all 14 locales (Orrery) `orrery/memory/project_i18n_ui_string_pipeline.md`
- **project-image-pipeline-v2** — "Post-#5 image pipeline state — sidecar manifests, walker fallback, override JSON, what consumes 1x1 (no 4x3/16x9 anymore)" `orrery/memory/project_image_pipeline_v2.md`
- **project_immersive_ar_150** — "Immersive Mode epic #150 — Exhibit Mode (#215) + AR core (#204-#214) + route wiring all built + local-tested on the `three-upgrade` branch as of 2026-07-11; NOT pushed; AR runtime needs the operator's devices" `orrery/memory/project_immersive_ar_150.md`
- **project_launch_ascent_epic** — "/fly launch/ascent epic — PRD-031/RFC-033/#412, S0 art direction locked; the pad→orbit act prepended to /fly" `orrery/memory/project_launch_ascent_epic.md`
- **project_launch_consolidation_plan** — "Launch/ascent consolidation into one extensible /fly system — ALL 8 slices IMPLEMENTED + verified 2026-07-18, uncommitted in working tree awaiting the operator's review" `orrery/memory/project_launch_consolidation_plan.md`
- **project_posters_higgsfield_regen** — "/posters gallery regeneration via Higgsfield — 3 locked styles, ~19-poster set, integration pending" `orrery/memory/project_posters_higgsfield_regen.md`
- **project_prod_public_exposure_vanilla** — "Orrery prod Phase-2 public exposure — vanilla self-hosted reverse proxy + Let's Encrypt, no vendor lock-in; Cloudflare CDN only as an optional front-plane toggle later" `orrery/memory/project_prod_public_exposure_vanilla.md`
- **project_prod_vps_data_refresh** — "Prod (orrerylearn.com VPS) self-refreshes launch data via on-VPS cron — the precompressed-/data gotcha + how it's wired" `orrery/memory/project_prod_vps_data_refresh.md`
- **project_programs_autonomous_roadmap** — "/programs build-out — SHIPPED TO MAIN (2026-07-12): 33 programs + full 14-locale i18n, merged to origin/main, all CI green incl docker-e2e" `orrery/memory/project_programs_autonomous_roadmap.md`
- **project_programs_factcheck_and_nav_essay_visuals** — "2026-07-14 uncommitted work — full /programs fact-check+fixes (all 42) and navigation essay visuals; preflight green, not pushed; Anthropic API usage limited until Aug 1" `orrery/memory/project_programs_factcheck_and_nav_essay_visuals.md`
- **project_science_localization_architecture** — "How /science localizes — getLocale() loads + complete per-locale overlays + per-locale prerender enumeration, NO en-US fallback hack" `orrery/memory/project_science_localization_architecture.md`
- **Science overlay translation pipeline + locale coverage** — How /science overlays get translated; which locales are done; the wave23 catalog/maps/apply-translations toolchain `orrery/memory/project_science_translation_pipeline.md`
- **project_sensory_layer_built** — "The Sensory Layer (haptics+sonification+gyro, #147) is built end-to-end in the working tree as of 2026-07-11 — replanned 'good simple' scope, preflight-green, NOT yet pushed" `orrery/memory/project_sensory_layer_built.md`
- **project_three_upgrade_203** — "Three.js r128→0.185.1 upgrade (#203/epic #150) is code-complete + preflight-green on the local `three-upgrade` branch as of 2026-07-11 — NOT pushed; merge needs CI baseline regen + e2e + the operator's go" `orrery/memory/project_three_upgrade_203.md`
- **v0.6.0 deploy state** — What's on origin/main and the gh-pages preview as of 2026-05-14 evening — the v0.6 release wave (suits + diagrams + fleet split + mobile UX). `orrery/memory/project_v06_deploy.md`
- **v0.7 started in parallel session (2026-05-16)** — v0.7 work kicked off with a different agent on 2026-05-16; check git state before assuming greenfield when picking up v0.7 tasks `orrery/memory/project_v07_parallel_session.md`

### orrery — reference (3)

- **reference-agency-primary-archives** — "Per-agency list of primary image sources for the agency-archive registry; Flickr is the de-facto primary for Roscosmos / CNSA / SpaceX where official sites are dark or have no public archive. Treat as a multi-primary list — try ALL in order, then Commons as failover." `orrery/memory/reference_agency_primary_archives.md`
- **reference_docker_tailnet_extrahosts_not_dns** — "Reaching a tailnet (Tailscale MagicDNS) backend from a docker container — use extra_hosts, NEVER dns:100.100.100.100" `orrery/memory/reference_docker_tailnet_extrahosts_not_dns.md`
- **fly-body-wiring-arrokoth-canonical** — Canonical worked example commit for adding a new flyby body (planet/dwarf/comet/asteroid/KBO) to /fly. 15-step checklist in TA.md §body-wiring + AGENTS.md. `orrery/memory/reference_fly_body_wiring_arrokoth.md`

### podcast_scraper — user (1)

- **User does not use 1Password** — Operator does NOT use 1Password CLI or app. Stop suggesting `op read`, `op signin`, `op://Personal/...` paths, or "save to 1Password" as a default workflow. `podcast_scraper/memory/user_no_1password.md`

### podcast_scraper — project (15)

- **Autoresearch Context** — Autoresearch v2 closed 2026-04-16 — authoritative refs, final defaults, what's next `podcast_scraper/memory/project_autoresearch.md`
- **project-autoresearch-programme** — Autoresearch programme map — #907 epic CLOSED (7 children done), #927 DGX championship CLOSED (all children #928-931 done); programme complete `podcast_scraper/memory/project_autoresearch_programme.md`
- **project_dgx_tailscale_acl** — "DGX tailscale ACL only allows ports 8003 + 11434 out to the mac — all research-side vLLMs (autoresearch, judge-a, judge-b) share port 8003" `podcast_scraper/memory/project_dgx_tailscale_acl.md`
- **project_dgx_vllm_distinction** — "Two distinct vLLM stacks on the operator's GB10 — `coder-next` (operator's IDE; off-limits to podcast_scraper) and `autoresearch` (live on vllm:26.05-py3, Qwen3-30B-A3B-Instruct-2507, :8003). Never conflate in eval reports, PROD_RUNBOOK rules, or contention sweeps." `podcast_scraper/memory/project_dgx_vllm_distinction.md`
- **project-diarization-1170-handover** — Handover for the `podcast_scraper/memory/project_diarization_1170_handover.md`
- **project-diarization-findings-1170** — The actual RESULTS/conclusions of the `podcast_scraper/memory/project_diarization_findings_1170.md`
- **project_goal1_golive_status** — "Goal-1 go-live execution status + resume plan (paused 2026-07-18) — segfault blocker, DR drill blocked, edge tooling authored on production" `podcast_scraper/memory/project_goal1_golive_status.md`
- **project-hostguest-swap-1169** — Host/guest role SWAPS in guest-heavy interviews — roster picks host by intro-window talk-time argmax; feed into `podcast_scraper/memory/project_hostguest_swap_1169.md`
- **ontology-v2-handoff** — KG+GI ontology v2 work — current state + identified gaps `podcast_scraper/memory/project_ontology_v2_handoff.md`
- **project-overview** — "podcast_scraper project — architecture, stack, purpose" `podcast_scraper/memory/project_overview.md`
- **project_player_live** — closelistening.app player is LIVE with coming-soon cookie gate + Google sign-in allowlist; verified end-to-end 2026-07-23 `podcast_scraper/memory/project_player_live.md`
- **project_player_sentry_dsn_split** — Player Sentry DSN split into _WEB/_API (2026-07-24); OLD PROD_SENTRY_DSN_PLAYER secret is orphaned — DELETE it `podcast_scraper/memory/project_player_sentry_dsn_split.md`
- **project_ranking_experiment_framework** — "Digest/player recommendation ranking must be a configurable experiment framework — every signal on/off + weight-tunable, centralized, for A/B testing" `podcast_scraper/memory/project_ranking_experiment_framework.md`
- **project_shared_public_edge** — "Shared multi-tenant public edge (Caddy) design + security SSOT — ADR-114, THREAT_MODEL.md, the T-01 docker.sock hole" `podcast_scraper/memory/project_shared_public_edge.md`
- **project_transcript_vs_audio_hosting** — Policy — podcast AUDIO is bridge-only (never rehost); transcript + all derived artifacts are ours to host/serve/transform; audio↔transcript sync is best-effort on dynamic-ad-insertion feeds `podcast_scraper/memory/project_transcript_vs_audio_hosting.md`

### podcast_scraper — reference (6)

- **dgx-judge-deployment-quirks** — "Pointer — judge-side vLLM deployment quirks (nemotron, qwen-next) live in the repo, not memory" `podcast_scraper/memory/reference_dgx_judge_deployment_quirks.md`
- **reference_glitchtip_dashless_dsn_key** — Browser @sentry SDK rejects GlitchTip dashed-UUID DSN keys; use dashless for browser surfaces (Python SDK accepts both) `podcast_scraper/memory/reference_glitchtip_dashless_dsn_key.md`
- **reference-gpu-mode-swap-script** — Where the gpu-mode-swap.sh script lives on the DGX host and how to reach it `podcast_scraper/memory/reference_gpu_mode_swap_script.md`
- **reference-per-model-optimal-params** — Canonical per-model vLLM-on-GB10 flag compendium. MUST consult before any multi-model sweep on DGX autoresearch slot. MUST update after every sweep with new findings. `podcast_scraper/memory/reference_per_model_optimal_params.md`
- **reference-pyannote-cache-offline** — pyannote 3.x vs 4.x use DIFFERENT model-cache dirs — the offline recipe for running diarization on the DGX with no HF download `podcast_scraper/memory/reference_pyannote_cache_offline.md`
- **session-reap-hook** — SessionEnd auto-reap kills session-started processes in the worktree so the machine returns to pre-session state `podcast_scraper/memory/reference_session_reap_hook.md`

---

**Totals:** 147 feedback rules, 152 verbatim operator quotes extracted.

