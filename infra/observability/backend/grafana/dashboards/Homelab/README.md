# Homelab — Grafana folder

Core infrastructure dashboards for the Mac mini observability platform (VictoriaMetrics/VictoriaLogs) and the DGX LLM host, scraped over the tailnet. Auto-provisioned into Grafana within 30s via `dashboards.yml`; see parent `../README.md` for provisioning model.

## Boards

| Board (uid) | Goal | Datasource | Key panels / vars |
|---|---|---|---|
| **Bug-fix Fleet** (`bugfix-fleet`) | Autonomous bug-fix pipeline visibility: issues by flow state (untriaged, shipped, stuck). Tracked metrics: `bugfix_fleet_issues_total`, `bugfix_fleet_issues{flow=...}`, `bugfix_fleet_issues_by_area`. | VictoriaMetrics | Total bugs (stat), Shipped/Stuck/Untriaged counts (stat), Pipeline funnel (bar gauge, grouped by flow), Bugs by area (pie), Pipeline states over time (series). No variables. |
| **Containers — cAdvisor** (`containers-cadvisor`) | Docker container resource usage: CPU (cores), memory (working set), network (RX/TX B/s) per container. Note: container names appear as short Docker IDs (tracked as GH #1272 — cAdvisor is missing the `name` label). | VictoriaMetrics | Containers seen / Total mem (stat), Top containers by memory (table), CPU/mem/net timeseries per container (all using `label_replace` to extract 12-char ID), Note panel warning. No variables. |
| **DGX — Services** (`dgx-services`) | DGX health, FastAPI workloads, support databases, vLLM queue depth. Scrapes `dgx_service_up`, `http_requests_total`, `http_request_duration_seconds_bucket`, `DCGM_FI_DEV_*`, `pg_up`, `redis_up`. | VictoriaMetrics | DGX app health (stat, `dgx_service_up{service=...}`), GPU util/mem (stat), FastAPI request rate / p95 latency (series, `$service` variable), vLLM running/waiting (series), Support DBs up (stat), Postgres connections (series). Var: `$service` (multi, queries `http_requests_total` jobs). |
| **GPU — DCGM** (`gpu-dcgm`) | NVIDIA GPU telemetry per device: utilization, temperature (core + memory), power, SM clock, memory-copy utilization, XID error count. | VictoriaMetrics | Five stat panels (util %, temp, power, mem-copy util, XID errors, all with thresholds), timeseries for GPU util % / temp / power / SM clock over 6h. Var: `$gpu` (multi, from `label_values(DCGM_FI_DEV_GPU_UTIL, gpu)`). |
| **Homelab — Mac mini** (`homelab-mini`) | Mac mini host metrics: CPU busy %, disk free %, memory used (active+wired+compressed), load (1m), swap, uptime. Also tracks thermal throttling ratio. | VictoriaMetrics | Six stat panels (disk free %, CPU busy, mem used %, load 1m, swap, uptime, all with thresholds), timeseries for CPU by mode / memory regions / disk free % / network RX|TX / disk I/O read|write / thermal speed-limit ratio. No variables. |
| **Logs — Overview** (`logs-overview`) | Aggregated log browser: total lines, error/warn count by regex, volume by container, live log stream. Supports container filtering and free-text search. | VictoriaLogs (`victoriametrics-logs-datasource`) | Total log lines / Errors & warnings (stat), Top containers by volume (table), Log volume by container (stacked bars), Errors & warnings over time (series), Live logs panel. Vars: `$container` (multi, from `field_values`), `$search` (text). |
| **Node Exporter Full** (`rYdddlPWk`) | Comprehensive host metrics (CPU, memory, disk, network, load, PSI, thermal). Vendored from grafana.com#1860. Provides deep drill-down on host resource state. | VictoriaMetrics | CPU / memory / disk rows with gauges, detailed timeseries for CPU modes, memory zones, I/O, network, thermal throttle. Uses `${ds_prometheus}` placeholder (auto-provisioned → `victoriametrics`). No variables. |
| **Orrery — launch data refresh** (`orrery-launch-data`) | Data refresh pipeline health for the Orrery LLM autoresearch service. Tracks successful / failed refresh attempts from logs over 24h. | VictoriaLogs | Successful refreshes (24h, stat), Failed refreshes (24h, stat), Live refresh job logs (log panel, filters `app:orrery job:orrery-data-refresh`). No variables. |

## Notes

- **cAdvisor container names (GH #1272):** Docker container identifiers show as short SHA substrings (e.g. `abc123def456`) because cAdvisor is not exporting the `name` label. Workaround: use the **Logs — Overview** dashboard for container-aware views (VictoriaLogs resolves the `container` field from log metadata).
- **Provisioning:** All boards are reloaded every 30s from disk via `foldersFromFilesStructure` in the Grafana provisioning config. UI edits persist if `allowUiUpdates: true`; export changes back to JSON to version them.
- **Datasources:** VictoriaMetrics (Prometheus-compatible metrics) and VictoriaLogs (LogsQL) run on the Mac mini at `:8428` and `:9428` respectively. DGX scrape targets (instance=`dgx-llm-1`) ship metrics over the tailnet/LAN.
- **Cross-reference:** See `../README.md` for vendoring rules, datasource config, and links to upstream sources (grafana.com, etc.).
